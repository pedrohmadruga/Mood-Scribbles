package com.example.moodscribbles.domain

/**
 * Primary mood scale for a journal entry (maps to calendar color / emoji in the UI).
 */
enum class Mood {
    VERY_SAD,
    SAD,
    NEUTRAL,
    HAPPY,
    VERY_HAPPY,
}
